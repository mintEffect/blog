---
layout: post
title: "온도 변화에 따른 곤충 개체 수"
date: 2023-09-01
last_modified_at: 2023-09-10
categories: [programming]
tags: [Processing]
---

```
double day=0.0000;
int startingValue = 0; // 변수의 초기값
int incrementAmount = 1; // 각 프레임에서 변수에 추가할 값
int timeInterval = 1000; // 시간 간격 (밀리초 단위)
int cnt=1;
int temp=0;
int lastx = 8, lasty = 8;
int survive = 1;
double[][] map = new double[49][49];
double randdayegg=0 , randdaylarva=0, randdaypupa=0;
double randcnt=0;
double randsurviveegg=0, randsurvivelarva = 0, randsurvivepupa=0;
double pday=0;
void keyPressed(){   //키보드가 입력됐는지 확인합니다.
  if(temp==0){
    if(key=='0'){
      temp = 15;
    }
    if(key=='1'){
      temp = 18;
    }
    if(key=='2'){
      temp = 20;
    }
    if(key=='3'){
      temp = 25;
    }
    if(key=='4'){
      temp = 30;
    }
  }
}
void setup() {
  size(1000, 1000); // 화면 크기 설정
  textSize(32);
}

void draw1() {
  background(220); // 배경색 설정

  int cols = 50; // 열 수
  int rows = 50; // 행 수

  float w = 800 / cols; // 각 격자의 너비
  float h = 800 / rows; // 각 격자의 높이
    
  for (int i = 0; i < cols; i++) {
    for (int j = 0; j < rows; j++) {
      float x = i * w; // x 좌표 계산
      float y = j * h; // y 좌표 계산
      
      // 각 격자를 그리기
      stroke(0); // 테두리 색상 (검정)
      noFill(); // 채우기 없음
      rect(x, y, w, h); // 사각형 그리기
    }
  }
  
  for(int i=0;i<49;i++){
    for(int j=0;j<49;j++){
      if(map[i][j]!=0){
               
      }
    }
  }
  if(temp!=0) day+=0.003;
   randsurviveegg = random(0,1000) / 10;
   randsurvivelarva = random(0,1000) / 10;
   randsurvivepupa = random(0,1000) / 10;
  if(temp==15){
    randdayegg = random(980,1060) / 100;
    randdaylarva = random(4370,4450) / 100;
    randdaypupa = random(2380,2900) / 100; 
    /*
    if(randsurviveegg>=796){
      survive = 0;
      fill(255,255,255);
      ellipse(lastx,lasty,16,16);
    }
    else{
      fill(0,255,0);
      ellipse(lastx,lasty,16,16);
    }
    if(randsurvivelarva>=310){
      survive = 0;
      fill(255,255,255);
      ellipse(lastx,lasty,16,16);
    }
    else{
      fill(0,0,255);
      ellipse(lastx,lasty,16,16);
      if(lastx<792){
        lastx+=16;
      }
      else{
        lastx = 8;
        lasty+=16;
      }
    }
    if(randsurvivepupa>=320){
      survive = 0;
      fill(255,255,255);
      ellipse(lastx,lasty,16,16);
    }
    else{
       fill(0,0,0);
      ellipse(lastx,lasty,16,16);
    }
    */
    pday = random(1360,1940) / 100;
  }
  if(temp==18){
    randdayegg = random(620,650) / 100;
    randdaylarva = random(3580,3640) / 100;
    randdaypupa = random(1280,1540) / 100;
    pday = random(1210,1750) / 100;
  }
  if(temp==20){
    randdayegg = random(580,620) / 100;
    randdaylarva = random(2810,3050) / 100;
    randdaypupa = random(1260,1500) / 100;
    pday = random(1220,1760) / 100;
  }
  if(temp==25){
    randdayegg = random(390,490) / 100;
    randdaylarva = random(1730,1910) / 100;
    randdaypupa = random(920,1040) / 100;
    pday = random(1090,1790) / 100;
  }
  if(temp==30){
    randdayegg = random(390,430) / 100;
    randdaylarva = random(1450,1610) / 100;
    randdaypupa = random(720,860) / 100;
    pday = random(900,1280) / 100;
  }
  
                                                                                                                      
  
  fill(255,0,0);  
  noStroke();
  ellipse(lastx,lasty,16,16); 
  fill(0);
  text("day : "+day, 810, 240);
  text("count : "+cnt,810,400);
  text("temp : "+temp,810,600);
}

```

draw1 draw2로

```
void draw1(){

}
void draw2(){

}
```

나눠서 하기